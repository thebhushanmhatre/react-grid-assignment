import { CopyText } from '../../CopyText';

export const EmailsCR = ({ value }: { value: string }) => {
  return <CopyText value={value} />;
};

// Creating this Department Cell Renderer for demonstration
